# Three working AI agents, public

**Source:** github.com/aditiventurewolf-max
**Attach when:** AI, agents, automation, applied LLM, or any founder's office
role at a company building with models.

## What to say when attaching it

"Three of these are public. A content discovery engine that does semantic search
over vector embeddings rather than keywords. A multi-agent market research tool
that takes a market and returns a report. And a recursive skill learner, where
Claude reads its own output, extracts what worked, and saves it as a new skill so
the next session starts better."

## What it proves

- Running code, not enthusiasm about AI.
- The recursive skill learner in particular is a real idea, not a wrapper.
- She specifies and evaluates agent systems rather than just using them. This job
  agent is the fourth: she caught that the first build had a running API cost and
  directed the rewrite onto a scheduled session instead.

## The fourth one, if it is relevant

The job search system itself. Discovery across six ATS APIs plus launch feeds,
free local filtering, a two-track pipeline for advertised roles and cold
approaches, a knowledge base that tracks which pitch angles get replies. Runs on
a schedule with no per-token cost.

> Adi: read this framing, fix anything wrong, then set status to `approved`.
